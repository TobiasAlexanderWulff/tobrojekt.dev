---
import * as page from '../../blogs/[slug].astro';

export const getStaticPaths = page.getStaticPaths;
const Component = page.default;
const { entry } = Astro.props as { entry: unknown };
---

<Component entry={entry} />
